1、 get 方法

   Hashmap get() 首先通过它的key的哈希码查找map的第一个节点，判断它是否是目标对象；不是则判断它是不是一个TreeNode，是，继续遍历树节点找到对应的key对象；否，则继续通过遍历next节点，找到目标对象。感觉HashMap的存储实现应该使用的是链表。
   
2、 put 方法

    存取对象是，首先判断第一个节点是否存在，不存在则创建，如果存在并且跟当前的key相同则替换原来的value。否则，判断是否一个TreeNode，是则存入TreeNode。否则，存在下一个节点。